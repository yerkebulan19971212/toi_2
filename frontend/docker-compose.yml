services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
      args:
        NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE:-https://api-toi.daraedu.kz}
    restart: always
    ports:
      - "3030:3030"
    environment:
      NUXT_PUBLIC_API_BASE: ${NUXT_PUBLIC_API_BASE:-https://api-toi.daraedu.kz}
