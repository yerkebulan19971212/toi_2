services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NUXT_PUBLIC_API_BASE: ${API_BASE:-http://localhost:8081}
    restart: always
    ports:
      - "3030:3030"
